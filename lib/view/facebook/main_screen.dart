import 'package:flutter/material.dart';

class FacebookMainScreen extends StatelessWidget {
  const FacebookMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Facebook"),
    );
  }
}