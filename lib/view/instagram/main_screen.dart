import 'package:flutter/material.dart';

class InstagramMainScreen extends StatelessWidget {
  const InstagramMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Instagram"),
    );
  }
}