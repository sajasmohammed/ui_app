import 'package:flutter/material.dart';

class LinkedInMainScreen extends StatelessWidget {
  const LinkedInMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("linked in"),
    );
  }
}