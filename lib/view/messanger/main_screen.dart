import 'package:flutter/material.dart';

class MessangerMainScreen extends StatelessWidget {
  const MessangerMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Messanger"),
    );
  }
}