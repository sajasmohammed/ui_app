import 'package:flutter/material.dart';

class WhatsAppMainScreen extends StatelessWidget {
  const WhatsAppMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Whats App"),
    );
  }
}