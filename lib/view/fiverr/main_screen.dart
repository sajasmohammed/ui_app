import 'package:flutter/material.dart';

class FiverrMainScreen extends StatelessWidget {
  const FiverrMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Fiverr"),
    );
  }
}