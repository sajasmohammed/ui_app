import 'package:flutter/material.dart';

class TelegramMainScreen extends StatelessWidget {
  const TelegramMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Telegram"),
    );
  }
}