import 'package:flutter/material.dart';

class PlaystoreMainScreen extends StatelessWidget {
  const PlaystoreMainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Playstore"),
    );
  }
}